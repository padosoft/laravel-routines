node_modules/
_site/
*.log
.DS_Store

# docmd-search: keep the pinned model config, ignore build cache/index artifacts
.docmd-search/*
!.docmd-search/config.json

# docmd-search build index written under the source tree at build time (regenerated each build)
docs/.docmd-search/
